/*
const newPromise = () => {

       let error = true;
       
       return new Promise((resolve, reject) => {
            
           if(!error) {
               resolve("Data Resolve successfully");
           }
           else {
              reject("There is some errro in data");
           }
             
            
       })
}

newPromise().then((data) => {
    console.log(data)
}).catch((error) => console.log(error))

*/































